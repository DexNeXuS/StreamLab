# Hello Text

Static text in a soft card style.

## Quick start

1. **Copy URL** and paste into OBS Browser Source, or **Download** and open the HTML file in OBS.
2. To change the text, download the file and edit the HTML: find the text inside the `.fade` (or main) div and replace it.

No URL parameters. Customise by editing the HTML.
